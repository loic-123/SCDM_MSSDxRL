# Minimal smoke test for CI — verifies pipeline works end-to-end
inherit:
  - defaults.yaml

shift_injection_step: 200       # Override: inject shift earlier for quick test
max_monitoring_steps: 1000      # Override: shorter monitoring horizon

experiment:
  name: "smoke_test"
  n_trials: 2
  shift_types: ["body"]
  include_no_shift: false
  parallel_workers: 1

envs:
  - envs/cliffwalking.yaml
  - envs/cartpole.yaml
